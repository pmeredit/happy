Arithmetic parsing producing a Abstract Syntax Dag.

Only two files are needed here:

Expr.y is the parser and lexer

Main.hs is the main driver of the final executable, it simply takes a string
inpunt from the command line and calls the paser on it then cleans up the
output.
